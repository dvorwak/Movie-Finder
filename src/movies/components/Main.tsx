import React from "react";
import MovieContainer from "./../containers/MovieContainer";

const Main = () => {
  return (
    <React.Fragment>
      <MovieContainer />
    </React.Fragment>
  );
};

export default Main;
